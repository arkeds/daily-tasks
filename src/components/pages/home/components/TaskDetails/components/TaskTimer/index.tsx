import { Paper } from "@mui/material";
import styles from "./tasktimer.module.scss";
const TaskTimer = () => {
  return (
    <Paper className={styles.task_timer_container} elevation={3}>
      Task
    </Paper>
  );
};

export default TaskTimer;
